name: CI

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Lint
        run: pnpm lint

      - name: Unit tests with coverage
        run: pnpm test:coverage

  integration:
    runs-on: ubuntu-latest
    services:
      clickhouse:
        image: clickhouse/clickhouse-server:24.8
        env:
          CLICKHOUSE_USER: default
          CLICKHOUSE_PASSWORD: clickflow_test
        ports:
          - 39487:8123
    env:
      CLICKFLOW_TEST_URL: http://127.0.0.1:39487
      CLICKFLOW_TEST_DATABASE: clickflow_ci
      CLICKFLOW_TEST_USER: default
      CLICKFLOW_TEST_PASSWORD: clickflow_test
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Wait for ClickHouse
        run: |
          for i in $(seq 1 40); do
            if curl -fsS "http://127.0.0.1:39487/ping" >/dev/null; then
              echo "ClickHouse is up"
              exit 0
            fi
            sleep 2
          done
          echo "ClickHouse did not become ready"
          exit 1

      - name: Integration tests
        run: pnpm test:integration
