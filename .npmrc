# Rollup (via tsup DTS) resolves native bindings as a sibling of `rollup` in node_modules.
# The default isolated linker often breaks that in CI/Docker; hoisting matches npm-like layout.
node-linker=hoisted
